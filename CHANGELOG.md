# 更新日志 / CHANGELOG

## 3.26.082015c（versionCode 10014）— 2026-08-20

### 十四、小悬浮窗默认一直停留
- **小悬浮窗停留时长**：可设 **0 = 一直停留**（不自动收进页脚），默认值改为 0——装上新版默认即一直停留
- 保留手动设置 1~300 秒的灵活性（设置为 0 即恢复一直停留）
- ⚠️ 已手动设置过时长的用户覆盖升级后保留原值，需在朗读设置中改为 0 才一直停留

### 十三、资源瘦身（不启用 R8，避免卡顿回归）
- **APK 体积 41.56MB → 32.17MB（省 9.4MB）**
- **背景图压缩**：`assets/bg` 13 张大图 JPG 重编码（`-q:v 8`，PSNR 39dB 肉眼无差），文件名不变，用户背景数据与 readConfig 完全兼容
- **删除死资源**：移除 `assets/web/help/` 页面资源（index.html + js + css，约 200KB，代码零引用），保留 17 个 md 帮助文档
- **语言裁剪**：`resConfigs "zh"` 只保留简体中文，第三方库语言资源一并裁剪（`resources.arsc` 3.5MB → 1.44MB），APK 内无任何语言变体目录
- 上述三项均为纯资源/构建层改动，未动 R8/混淆/反射链（吸取 10009 卡顿教训）

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.082015c_10014_arm64-v8a.apk` | `io.legado.app.c` | 正式版（32.17MB，可覆盖升级，保留数据） |

---

## 3.26.0819c（versionCode 10012）— 2026-08-20

### 十二、移植 legadoC 听书融合 + 墨水屏

#### 听书功能（legadoC 8-17 融合 + 8-18 优化）
- **有声小说与普通小说融合**：有声书从书籍详情/目录进入普通阅读页，不再分流独立页面；数据层区分媒体类型（同名不同媒体不互相覆盖）
- **统一朗读引擎**：书源音频 / 系统 TTS / HTTP TTS 平级接入（`ReadAloud` / `BaseReadAloudService`），引擎决定进度语义；书源音频新增 `SourceAudioReadAloudService`
- **文字-音频双向映射**：双击/点击段落/指哪读哪，文本段落 ↔ 音频位置互跳
- **朗读页脚小面板**：阅读页底部控制块（暂停/继续、原进度/从本页听）直接生效；小悬浮窗停留时长默认 10s、自动收进页脚中央
- **朗读封面旋转**：开关默认开 + 旋转一圈用时默认 10s，悬浮窗与听书页封面同步旋转
- **听书页优化**：移除第三方歌词视图依赖、统一文本渲染与点句跳转、朗读跟随延迟默认 3000ms、进度轮询间隔默认 500ms、正文居中与首行缩进修复、音频缓存通知章节进度
- **离线缓存 + 引擎切换**：听书页更多菜单新增入口；有声书默认语速 1.6
- 删除旧听书三件套：`AudioPlay` / `AudioPlayService` / `AudioPlayViewModel`（被统一引擎替代）

#### 墨水屏模式（8-18）
- 自动/日间/夜间/墨水屏切换统一走单一入口；墨水屏下弹窗表面强制不透明

#### 其他
- **AI 章节净化完整保留**（含全部 UI 入口，未剥离）
- 数据库版本 100 → 104（Room 自动迁移，已装 10011 可覆盖升级保留数据）
- 修复 legadoC 上游 bug：`AudioPlayActivity` 中不存在的 `StaticLayout.Builder.release()` 调用

#### ⚠️ 阅读进度同步修复（重点）
- **纯位置比较**：`BookProgress.compareWith` 移除时间戳优先，只比较 `durChapterIndex` → `durChapterPos`，避免多设备因本地时间戳差异互相覆盖
- **进度文件名兼容**：`AppWebDav.getProgressFileName` 恢复 `书名_作者.json` 无后缀命名，兼容 WebDAV 服务器既有进度文件
- **退出自动上传**：`ReadBookActivity` / `ReadMangaActivity` 的 onPause 同步移除 `BuildConfig.DEBUG` 限制（DEBUG 构建也自动同步）

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.0819c_10012_arm64-v8a.apk` | `io.legado.app.c` | 正式版（37.8MB，可覆盖升级，保留数据） |

---

## 3.26.0816c（versionCode 10011）— 2026-08-19

### 十一、界面透明度优化（默认 0 / 20% / 50%）

- **全局界面透明度强制为 0 且不可调整**：界面设置中移除该选项；`AppConfig.uiLayoutAlpha` 现在恒返回 0、setter 为空操作；主题包编辑页隐藏该行；主题包导入/导出与备份均不再携带该值（避免被旧主题包覆盖回非零）
- **弹窗透明度默认 20%**（原默认 15%，`AppConfig.dialogAlpha` 与偏好默认值同步，`pref_config_theme.xml` 默认值改为 20）
- **书架封面透明度默认 20%**（原默认 15%，同上）
- **弹窗模糊度默认 50%**（原默认 100，同上）
- 三处默认值均保留可调；仅全局界面透明度被锁死为 0（保持完全不透明底图，规避半透明叠加带来的视觉与性能问题）
- 改动文件：`AppConfig.kt`、`ThemeConfigFragment.kt`、`ThemeManageActivity.kt`、`pref_config_theme.xml`、`ThemeConfig.kt`、`BackupConfig.kt`

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.0816c_10011_arm64-v8a.apk` | `io.legado.app.c` | 正式版（36.8MB，可覆盖升级，保留数据） |

---

## 3.26.0816c（versionCode 10010）— 2026-08-18

### 十、回退 R8 裁剪，保留 WebP 图片优化（体积 24.6MB → 35.1MB）

- **回退原因**：10009 开启 R8 混淆后实测出现界面卡顿、动画不流畅、操作不跟手。根因是 legado 重度依赖反射（书源引擎 / JS 桥 / 动态类加载），R8 混淆重命名类后运行时反射调用频繁失败（静默异常拖慢主线程），且资源收缩误删了部分系统过渡动画
- **改动**：`c` 变体 `minifyEnabled` / `shrinkResources` 恢复 `false`（与 10008 一致），dex 恢复 23 个，运行时反射链完整
- **保留的优化**：默认封面图 WebP 优化保留（2.53MB → 121KB，纯资源替换，无任何性能影响）
- 此版本与 10008 功能一致、运行流畅，仅体积比 10008 小 2.4MB

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.0816c_10010_arm64-v8a.apk` | `io.legado.app.c` | 正式版（35.1MB，可覆盖升级，保留数据） |

> 提示：从旧版（release 签名）升级时若遇签名不兼容，可用本仓库提供的数据迁移方案（详见 README），或先在应用内备份导出再导入。

---

## 3.26.0816c（versionCode 10009）— 2026-08-18

### 九、安装包精简（体积 37.5MB → 24.6MB，-34%）

- **开启 R8 代码裁剪 + 资源收缩**（`minifyEnabled true` + `shrinkResources true`）：dex 从 24 个降至 4 个，未使用的资源自动剔除
- **补充 WebView JS 桥 keep 规则**：`WebCacheManager` / `WebViewActivity$JSInterface` / `ReadRssActivity$JSInterface` 方法名不混淆，保证 Web 阅读/浏览器书源注入正常
- **默认封面图转 WebP**：`image_cover_default_night.png`（2.53MB）→ WebP q85（121KB），代码引用不变，视觉无差异
- 其余沿用 legadoC 上游自带 172 行 proguard 规则（JS 引擎 / 数据类 / 书源引擎 / Jsoup / 播放器 / 语法高亮等）
- ⚠️ R8 裁剪涉及重反射应用，**新装后请重点回归**：书源搜索 / 换源 / 朗读 / WebDAV 同步 / 漫画 / Web 阅读

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.0816c_10009_arm64-v8a.apk` | `io.legado.app.c` | 正式版（24.6MB，可覆盖升级，保留数据） |

> 提示：从旧版（release 签名）升级时若遇签名不兼容，可用本仓库提供的数据迁移方案（详见 README），或先在应用内备份导出再导入。

---

## 3.26.0816c（versionCode 10008）— 2026-08-17

### 八、移除默认背景壁纸

- **删除内置默认背景壁纸**：全新安装不再自动设置彩色底图（原 `pre_default_background.png` 已移除），主题背景为纯色，界面更简洁
- 已有数据的设备不受影响（背景设置保留用户自己的选择）

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.0816c_10008_arm64-v8a.apk` | `io.legado.app.c` | 正式版（可覆盖升级，保留数据） |

> 提示：从旧版（release 签名）升级时若遇签名不兼容，可用本仓库提供的数据迁移方案（详见 README），或先在应用内备份导出再导入。

---

## 3.26.0816c（versionCode 10007）— 2026-08-17

### 七、更新入口整合与主题默认化

- **「我的」界面移除「更新设置」栏**：更新相关设置不再单独成栏，入口整合到「关于」页面
- **「关于」页新增「立即检查更新」**：一键手动检测 GitHub（skxingyu/legado-sk）新版本，不再包含自动检测开关、加速源等冗余选项；应用启动不再自动检查更新
- **界面设置默认值调整**：弹窗模糊度默认 50%，弹窗透明度 / 全局界面透明度 / 书架封面透明度默认 20%
- **默认主题包替换**：移除阅读C 继承的彩色主题（「默认」红色等），内置默认主题改为「黑猫慢生活」（日间）+「黯夜」（夜间），全新安装默认即为黑猫慢生活配色
- **正式版发布（仅 c 变体）**：本版本起不再提供 sk2 共存测试版

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.0816c_10007_arm64-v8a.apk` | `io.legado.app.c` | 正式版（可覆盖升级，保留数据） |

> 提示：从旧版（release 签名）升级时若遇签名不兼容，可用本仓库提供的数据迁移方案（详见 README），或先在应用内备份导出再导入。
> 说明：内置默认主题/阅读样式仅对全新安装（无用户数据）生效；已有数据的设备可在 界面设置 → 主题 中手动选择内置的「黑猫慢生活」等主题。

---

## 3.26.0816c（versionCode 10006）— 2026-08-17

### 六、更换书源界面恢复旧版观感

- **「更换书源」「章节换源」对话框恢复 0813 样式**：换源对话框改用旧版容器样式（圆角表面色背景 + 暗色模式下背景 12% 白色提亮），告别新材质样式，观感更清爽
- **仅影响换源对话框**：其他对话框（书源管理、设置弹窗等）保持原样，通过内部开关隔离，无副作用

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.0816c_10006_arm64-v8a.apk` | `io.legado.app.c` | 正式版（可覆盖升级，保留数据） |
| `legado_sk_3.26.0816sk2_10006_arm64-v8a.apk` | `io.legado.app.sk2` | 共存测试版（应用名「阅读SK」） |

> 提示：从旧版（release 签名）升级时若遇签名不兼容，可用本仓库提供的数据迁移方案（详见 README），或先在应用内备份导出再导入。
> 说明：内置默认阅读样式仅对全新安装（无用户数据）生效；已有数据的设备可在 阅读界面 → 样式 中手动选择内置的「猫咪」等方案。

---

## 3.26.0816c（versionCode 10005）— 2026-08-17

### 一、主题背景默认化（默认即用）

- **内置默认阅读样式改为个人方案**：内置「猫咪」「秋」「春」「猫黄」「黑猫」5 套阅读样式，背景图随应用内置（assets 资源），无需联网或手动导入
- **默认阅读样式为「猫咪」**：全新安装后打开阅读界面即为猫咪背景（含夜间模式背景）
- **内置默认主题扩展为 6 套**：新增「黑猫慢生活」「黯夜」，原有「默认 / 典雅蓝 / 黑白 / A屏黑」保留
- **主题模式默认浅色**：`themeMode` 默认值由「跟随系统」改为「浅色」

### 二、更新策略调整

- **自动检测更新默认关闭**：应用启动时不再自动检查更新（可在 设置 → 其他 中手动开启），避免每次启动弹更新提示
- 更新检查源与更新日志均指向本项目仓库（skxingyu/legado-sk）

### 三、阅读进度同步增强（移植 legado-E）

- **修复批量拉取提前中断 bug**：`AppWebDav.downloadAllBookProgress` 中 `return` → `return@forEach`，多本书进度拉取不再因第一本不符合条件而中断
- **进度比较改为纯位置比较**：`ReadBook` / `ReadManga` / `ReadBookViewModel` / `ReadMangaViewModel` 的同步判断从 `when(compareWith)` 改为基于 `durChapterIndex` / `durChapterPos` 的位置比较，同步更准确
- **DEBUG 构建也自动同步**：`ReadBookActivity` / `ReadMangaActivity` 的 `onPause` 移除 `!BuildConfig.DEBUG` 限制，调试版同样自动同步/上传进度
- **同步开关默认开启**：`syncBookProgressPlus` 默认值改为 `true`

### 四、朗读悬浮窗优化

- **阅读界面内隐藏悬浮窗**：朗读（听书）时，小说阅读界面（ReadBookActivity）内不再显示悬浮窗，避免遮挡阅读内容；退出阅读界面后在应用内其他页面显示（App 内悬浮窗），应用退到后台不显示
- **新增配置项**：`阅读界面内隐藏悬浮窗`（`readAloudHideFloatingInReadBook`，默认开启），位于 设置 → 朗读配置；依赖「隐藏朗读悬浮窗」关闭时生效，可随时关闭恢复原行为
- **朗读控制不受影响**：阅读界面内仍可通过朗读菜单（ReadAloudDialog）暂停/继续/停止

### 五、品牌与文档

- 应用名统一为「阅读SK」（正式版 `io.legado.app.c` 桌面名由「阅读 C」改名为「阅读SK」）
- 「关于」页移除 QQ 群入口与相关文案（不含任何交流群）
- 「关于」页更新日志改为从本项目仓库（skxingyu/legado-sk）拉取
- 应用内更新检查源改为本项目仓库的 Releases
- README 重写为项目介绍 + 主要优化方向说明，并致谢 legadoC / legado 原作者

### 安装包

| 文件 | 包名 | 说明 |
|---|---|---|
| `legado_sk_3.26.0816c_10005_arm64-v8a.apk` | `io.legado.app.c` | 正式版（可覆盖升级，保留数据） |
| `legado_sk_3.26.0816sk2_10005_arm64-v8a.apk` | `io.legado.app.sk2` | 共存测试版（应用名「阅读SK」） |

> 提示：从旧版（release 签名）升级时若遇签名不兼容，可用本仓库提供的数据迁移方案（详见 README），或先在应用内备份导出再导入。
> 说明：内置默认阅读样式仅对全新安装（无用户数据）生效；已有数据的设备可在 阅读界面 → 样式 中手动选择内置的「猫咪」等方案。
